# PyTorch 系列教程重写规范

> 目标：把当前偏短、偏提纲式的教程，统一升级成“能讲明白理论、能落地实操、结构清晰、图文并茂、代码注释充分”的增强版教程。

## 一、每篇教程必须满足的硬性要求

每一篇教程都必须包含下面 6 个部分，不能只给最小示例。

### 1. 学习目标
- 本篇会解决什么问题
- 学完之后能做什么
- 与前后章节的关系是什么

### 2. 理论讲解
- 解释核心概念是什么
- 解释它为什么存在、解决什么问题
- 说明它和相关概念的区别
- 不只给定义，要给直觉理解

### 3. 真实使用场景
- 说明真实项目里什么时候会用到
- 举一个贴近工程实践的例子
- 尽量使用本地可运行、非空洞的场景

### 4. Mermaid 图示
- 每篇至少 1 个 mermaid 图
- 优先使用 `flowchart`、`sequenceDiagram`、`classDiagram` 中最适合的一种
- 图必须服务于理解，不是为了凑图
- 图中节点命名要清晰，尽量中文化

### 5. 真实代码片段
- 代码不是伪代码，应该能在本地运行或仅需少量改动即可运行
- 每段核心代码都要有详细中文注释
- 代码前说明“这段代码要做什么”
- 代码后解释“输出结果怎么看”

### 6. 常见问题与练习
- 本篇常见报错或误区
- 至少给出 3 个练习题
- 练习题从基础到稍微开放一点递进

---

## 二、推荐统一结构

每篇教程建议采用下面结构：

```markdown
# EPxx｜标题

## 本篇你会学到什么

## 一、为什么要学这个

## 二、核心理论讲解

## 三、先建立一个直觉理解

## 四、真实项目里怎么用

## 五、流程图 / 结构图（Mermaid）

## 六、从零写一个最小可运行示例

## 七、拆解代码执行过程

## 八、运行结果应该怎么看

## 九、常见错误与排查

## 十、本篇小结

## 十一、练习题
```

---

## 三、写作风格要求

### 1. 不要只写“是什么”，要写“为什么”
差的写法：
- Tensor 是多维数组。

更好的写法：
- Tensor 可以理解成带有 dtype、device、梯度信息的多维数组。PyTorch 用它统一承载数据、参数和梯度，这样训练和推理流程就可以在同一套数据结构上运行。

### 2. 不要只给 API 罗列
差的写法：
- `reshape()` 用来变形，`view()` 也能变形，`unsqueeze()` 增加维度。

更好的写法：
- 先解释“为什么模型在不同阶段需要不同 shape”，再引出 `reshape()`、`view()`、`unsqueeze()` 的用途和区别。

### 3. 代码注释必须解释“意图”
不要只写：
- `# 定义模型`

更好的注释：
- `# 这里把 10 维输入先映射到 32 维隐藏层，本质上是在给模型一个更高维的特征表达空间`

### 4. 面向真实学习者
默认读者：
- 会一点 Python
- 刚接触或正在入门 PyTorch
- 想把教程内容真正跑起来，而不是只看概念

---

## 四、Mermaid 图的使用要求

### 1. 什么时候用流程图
适合：
- 训练循环
- 数据流
- 推理流程
- 保存/加载流程

### 2. 什么时候用时序图
适合：
- autograd 反向传播过程
- DataLoader 与模型之间的交互流程

### 3. 什么时候用结构图
适合：
- 模型模块组成
- 项目目录结构
- 训练模板架构

### 4. Mermaid 示例

```mermaid
flowchart LR
    A[原始输入数据] --> B[Dataset 返回样本]
    B --> C[DataLoader 组装 Batch]
    C --> D[模型前向传播]
    D --> E[计算损失]
    E --> F[反向传播]
    F --> G[优化器更新参数]
```

---

## 五、代码片段规范

### 1. 每段代码前要交代用途
例如：
- 下面这段代码演示如何创建不同 dtype 和 device 的 Tensor，并观察它们在计算中的行为差异。

### 2. 每段代码内部注释要充分
尤其要解释：
- 输入输出 shape
- dtype 要求
- 为什么要这样写
- 哪些地方容易报错

### 3. 每段代码后要说明观察点
例如：
- 运行后重点观察 `shape`、`dtype`、`device` 三项输出。
- 如果你把 `float32` 改成 `int64`，部分计算行为会发生变化。

---

## 六、长度建议

为避免“每篇都太短”，建议：

- 基础篇：至少 1500~2500 字
- 实战篇：至少 2000~3500 字
- 项目篇：至少 2500~4000 字

不是为了凑字数，而是确保：
- 理论能讲清
- 示例能讲透
- 常见坑能覆盖

---

## 七、改写优先级建议

优先加强下面几篇，因为它们最需要“理论 + 实战”结合：

1. EP02 Tensor 与张量基本操作
2. EP03 自动求导与反向传播
3. EP05 Dataset 与 DataLoader 数据管道
4. EP06 完整训练循环实战
5. EP10 模型保存、加载与断点续训
6. EP12 GPU、混合精度与训练加速
7. EP15 项目实战：整理一个可复用训练模板

---

## 八、改写验收标准

当一篇教程满足下面条件时，视为合格：

- [ ] 不再只是提纲式内容
- [ ] 有成体系的理论解释
- [ ] 有真实使用场景
- [ ] 至少 1 个 Mermaid 图
- [ ] 至少 1 段带详细中文注释的真实代码
- [ ] 有运行结果解释
- [ ] 有常见问题和练习题
- [ ] 本地 Windows 环境读者能直接照着做
