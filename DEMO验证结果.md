# demo 验证记录

这个文件就是留个底，说明这套教程里的 demo 到底哪些真跑过，哪些只是文档里写了但还没单独拆脚本。

## 当时用的环境

验证时用的是单独拉出来的环境，不是系统里原来那套 Python：

- Python 3.12
- PyTorch 2.12.0+cpu
- Torchvision 0.27.0
- Pillow 12.2.0

当时这么做的原因很简单：系统原环境里 `torch` 能导，但是 `torchvision + PIL` 有 DLL 问题，直接跑会炸，所以另外建了 `.venv_demo` 来验证。

不过为了仓库干净，那个环境后来已经删掉了。

---

## 实际跑过的部分

### 1）前 5 篇基础内容
用的是：
- `demo_validate_ep01_ep05.py`

这里实际覆盖了：
- EP01 环境自检
- EP02 Tensor 基本操作
- EP03 自动求导 / 梯度
- EP04 `nn.Module` 最小网络
- EP05 `Dataset` / `DataLoader`

这一段属于最基础的 PyTorch 入门链路，已经实际跑通过。

### 2）后面那批核心链路
用的是：
- `demo_validate_ep06_ep15.py`

这里实际测过：
- EP06 最小训练循环
- EP08 CNN 结构和输出 shape
- EP09 准确率计算
- EP10 模型保存、加载、checkpoint 恢复
- EP12 device / autocast / GradScaler 结构
- EP13 单图推理链路
- EP15 模板目录最小落地和 `infer.py` 冒烟测试

也就是说，训练、保存、推理、模板这条线不是纸上写写，是真跑过一遍的。

---

## 后来单独补测过的内容

### EP07
这篇是 MNIST。

后面单独测过：
- `torchvision.datasets.MNIST(..., download=True)` 能下载
- 数据能正常读取
- 取样本时 shape 正常

当时拿到的结果大概是：
- 数据集长度：`60000`
- 样本 shape：`torch.Size([1, 28, 28])`

### EP11
这篇后来补成了两种模式，而且两种都测过。

#### 离线结构验证
- `weights=None`
- 能正常替换 `fc`
- 能检查冻结参数逻辑

#### 在线预训练权重
- `weights=ResNet18_Weights.DEFAULT`
- 权重实际下载成功过
- 分类头替换也正常

所以这篇现在不只是“理论上能用”，而是在线和离线两条路都走通过。

---

## 这次实际修过的问题

### EP01 命令写坏过
一开始虚拟环境激活命令里混进了控制字符，后来已经修掉了，现在是正常的：

```powershell
.\.venv\Scripts\activate
```

### 原系统 Python 环境有依赖问题
不是文档的问题，是本机原环境的问题：
- `torch` 可以导入
- `torchvision` 触发 `PIL._imaging` DLL 错误

所以后面验证都放在独立环境里做了。

---

## 现在怎么理解这份记录

这份记录不是测试报告，也不是 CI 日志。
它更像一个人工维护备注，主要说明两件事：

1. 这套教程不是只写了文档，核心 demo 确实跑过
2. 跑的过程中发现过什么坑，后来怎么处理的

如果后面还继续扩展这套教程，建议保持这个文件的风格：
- 有跑过就写清楚
- 没跑过别硬说通过
- 真遇到的问题就直接记下来
